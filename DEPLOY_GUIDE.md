# 📦 明升伟业网站 - 部署指南

## 🚀 快速部署方案（3选1）

---

## ✅ 方案1：Netlify CLI 部署（推荐）

### 执行步骤：

1. **双击运行脚本**：
   ```
   deploy-netlify.bat
   ```

2. **浏览器会自动打开**，显示授权页面：
   - 点击 "Authorize"
   - 等待显示 "Success!"
   - 关闭浏览器回到命令行

3. **等待部署完成**，会显示：
   ```
   ✅ Live URL: https://xxx.netlify.app
   ```

### 如果遇到登录问题：

**方法A - 使用邮箱登录：**
```bash
netlify login --email your-email@example.com
```
然后检查邮箱，点击授权链接。

**方法B - 手动获取 Token：**
1. 访问：https://app.netlify.com/user/applications
2. 点击 "New access token"
3. 复制 Token
4. 执行：
   ```bash
   netlify login --token YOUR_TOKEN
   ```

---

## ⚡ 方案2：Surge 部署（最简单）

### 执行步骤：

1. **双击运行脚本**：
   ```
   deploy-surge.bat
   ```

2. **首次使用需要注册**：
   ```
   email: 输入你的邮箱
   password: 输入密码（不会显示，直接输入即可）
   ```

3. **等待上传完成**，会显示：
   ```
   Success! Published to https://mingsheng-website.surge.sh
   ```

### 优点：
- ✅ 无需浏览器授权
- ✅ 只需邮箱密码
- ✅ 部署速度快
- ✅ 完全免费

---

## 🌐 方案3：GitHub + Netlify/Vercel（自动部署）

### 步骤1：推送到 GitHub

```bash
# 如果还没有创建远程仓库，去 GitHub 创建一个
# https://github.com/new

# 添加远程仓库
git remote add origin https://github.com/你的用户名/mingsheng-website.git

# 推送代码
git push -u origin main
```

### 步骤2：在 Netlify/Vercel 导入项目

**Netlify：**
1. 访问：https://app.netlify.com
2. 点击 "Import from Git"
3. 选择 GitHub 仓库
4. 配置：
   - Build command: `npm run build`
   - Publish directory: `dist`
5. 点击 "Deploy"

**Vercel：**
1. 访问：https://vercel.com
2. 点击 "Add New..." → "Project"
3. 选择 GitHub 仓库
4. 配置自动识别，直接点 "Deploy"

### 优点：
- ✅ 推送代码自动部署
- ✅ 无需每次手动操作
- ✅ 支持预览分支
- ✅ 免费 HTTPS + CDN

---

## 🔍 常见问题解决

### Q1: Netlify 登录一直转圈？

**解决方法：**
1. 清除浏览器缓存
2. 使用无痕模式
3. 或使用 Token 登录：
   ```bash
   netlify login --token YOUR_TOKEN
   ```

### Q2: Surge 提示 "command not found"？

**解决方法：**
```bash
npm install -g surge
```

### Q3: 网站访问 404 错误？

**原因：** 路由配置问题

**解决方法：** 
- Netlify：已配置 `netlify.toml`
- Vercel：已配置 `vercel.json`
- Surge：在 `dist` 目录添加 `ROUTER` 文件

### Q4: 如何自定义域名？

**Netlify：**
1. 在项目设置中点击 "Domain settings"
2. 点击 "Add custom domain"
3. 输入你的域名
4. 在域名注册商添加 CNAME 记录

**Vercel：**
1. 项目设置 → Domains
2. 添加你的域名
3. 配置 DNS 解析

---

## 📊 方案对比

| 方案 | 速度 | 难度 | 特点 |
|------|------|------|------|
| **Netlify CLI** | 2分钟 | ⭐⭐ | 功能强大，自动 HTTPS |
| **Surge** | 1分钟 | ⭐ | 超级简单，一条命令 |
| **GitHub 自动部署** | 5分钟 | ⭐⭐⭐ | 推送自动部署，长期使用推荐 |

---

## 🎯 推荐方案

- **演示/测试**：用 **Surge**（最快）
- **正式上线**：用 **Netlify CLI** 或 **GitHub + Netlify**
- **团队协作**：用 **GitHub + Vercel/Netlify**（自动部署）

---

## 📞 需要帮助？

遇到问题，可以：
1. 查看部署日志
2. 检查 `dist` 目录是否有文件
3. 确认 `index.html` 存在
4. 查看浏览器控制台错误

---

**最快开始：双击 `deploy-surge.bat` 即可！** 🚀
