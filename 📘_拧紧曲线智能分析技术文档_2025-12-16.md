# 📘 拧紧曲线智能分析技术文档

**版本**: v3.0.0  
**日期**: 2025-12-16  
**作者**: 智能体助手 + 拧紧工艺专家团队

---

## 📋 目录

1. [系统架构](#系统架构)
2. [核心算法](#核心算法)
3. [数据结构](#数据结构)
4. [异常检测逻辑](#异常检测逻辑)
5. [关键点识别](#关键点识别)
6. [材质识别算法](#材质识别算法)
7. [性能优化](#性能优化)
8. [API接口](#api接口)
9. [配置参数](#配置参数)
10. [扩展开发](#扩展开发)

---

## 1. 系统架构

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端展示层 (Vue 3)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ 曲线图表  │  │ 筛选器   │  │ 分析结果 │  │ 报告导出 │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
└─────────────────────────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    数据处理层 (JavaScript)                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │异常检测  │  │特征提取  │  │对比分析  │  │报告生成  │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
└─────────────────────────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────┐
│                   知识库层 (knowledgeBase.js)             │
│  ┌──────────────────────────────────────────────────┐   │
│  │  - 三阶段理论 (5000字)                            │   │
│  │  - 7种异常诊断手册 (6000字)                      │   │
│  │  - 材质识别系统 (4000字)                         │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 1.2 技术栈

| 层次 | 技术 | 用途 |
|------|------|------|
| 前端框架 | Vue 3 + Composition API | 组件化开发 |
| 图表库 | ECharts 5.x | 曲线可视化 |
| UI组件 | Element Plus | 交互组件 |
| 状态管理 | Reactive API | 响应式数据 |
| 数学计算 | 原生JavaScript | 异常检测算法 |
| 数据存储 | LocalStorage | 客户端缓存 |

---

## 2. 核心算法

### 2.1 异常检测主流程

```javascript
function detectCurveAnomalies(curve) {
  // 1. 数据验证
  if (!curve.data || curve.data.length < 100) {
    return '数据不足'
  }
  
  // 2. 特征提取
  const features = extractCurveFeatures(curve.data)
  const seatPoint = features.seatPoint
  const elasticSlope = features.elasticSlope
  
  // 3. 异常检测
  const anomalies = []
  
  // 3.1 滑牙检测
  if (detectSlipAnomaly(curve.data, maxTorque)) {
    anomalies.push('滑牙')
  }
  
  // 3.2 粘滑检测
  if (detectStickSlipAnomaly(curve.data)) {
    anomalies.push('粘滑')
  }
  
  // 3.3 缓升检测
  if (elasticSlope < THRESHOLDS.slowRiseSlope) {
    anomalies.push('缓升')
  }
  
  // 3.4 压溃检测
  if (detectCrushAnomaly(curve.data)) {
    anomalies.push('压溃')
  }
  
  // 3.5 断裂检测
  if (detectBreakAnomaly(curve.data)) {
    anomalies.push('断裂')
  }
  
  // 3.6 浮钉检测
  if (seatPoint.angle > THRESHOLDS.seatPointMaxAngle) {
    anomalies.push('浮钉')
  }
  
  // 3.7 开裂检测
  if (detectCrackAnomaly(curve.data)) {
    anomalies.push('开裂')
  }
  
  // 4. 返回状态
  return anomalies.length === 0 
    ? '正常' 
    : `异常-${anomalies.join('/')}`
}
```

### 2.2 贴合点识别算法

```javascript
/**
 * 贴合点 (Seat Point) 识别
 * 定义: 扭矩从0开始持续上升超过阈值的首个点
 * 
 * 物理意义:
 * - 螺栓头部与工件表面完全贴合
 * - 有效拧紧的起点
 * - 用于计算有效转角
 */
function findSeatPoint(data) {
  const TORQUE_THRESHOLD = 5 // N·m
  const MIN_ANGLE = 200 // 度(排除干扰)
  
  for (let i = 0; i < data.length; i++) {
    const [angle, torque] = data[i]
    
    // 条件:
    // 1. 角度 > 200° (排除初期干扰)
    // 2. 扭矩 > 5 N·m (有效扭矩)
    // 3. 后续持续上升(验证)
    if (angle > MIN_ANGLE && torque > TORQUE_THRESHOLD) {
      // 验证后续10个点是否持续上升
      let isContinuousRise = true
      for (let j = i + 1; j < Math.min(i + 10, data.length); j++) {
        if (data[j][1] < torque) {
          isContinuousRise = false
          break
        }
      }
      
      if (isContinuousRise) {
        return {
          index: i,
          angle: angle,
          torque: torque
        }
      }
    }
  }
  
  return { index: 0, angle: 0, torque: 0 }
}
```

### 2.3 弹性段斜率计算

```javascript
/**
 * 弹性段斜率 (Elastic Slope) 计算
 * 定义: 360-480°区间的扭矩-角度变化率
 * 
 * 物理意义:
 * - 反映连接件刚度
 * - 用于材质识别
 * - 评估连接质量
 * 
 * 公式: slope = ΔT / Δθ
 */
function calculateElasticSlope(data) {
  const ELASTIC_START = 360 // 度
  const ELASTIC_END = 480   // 度
  
  // 提取弹性段数据
  const elasticData = data.filter(
    ([angle, torque]) => angle >= ELASTIC_START && angle <= ELASTIC_END
  )
  
  if (elasticData.length < 2) return 0
  
  // 线性回归计算斜率
  const n = elasticData.length
  let sumX = 0, sumY = 0, sumXY = 0, sumX2 = 0
  
  elasticData.forEach(([x, y]) => {
    sumX += x
    sumY += y
    sumXY += x * y
    sumX2 += x * x
  })
  
  // 最小二乘法: slope = (n*ΣXY - ΣX*ΣY) / (n*ΣX² - (ΣX)²)
  const slope = (n * sumXY - sumX * sumY) / (n * sumX2 - sumX * sumX)
  
  return slope
}
```

### 2.4 屈服点识别算法

```javascript
/**
 * 屈服点 (Yield Point) 识别
 * 定义: 曲线从直线开始明显偏离,斜率减小的点
 * 
 * 物理意义:
 * - 螺栓开始塑性变形
 * - 超过此点可能损坏
 * - 用于屈服点控制策略
 * 
 * 算法: 二阶导数检测法
 */
function findYieldPoint(data) {
  const WINDOW_SIZE = 10
  const SLOPE_REDUCTION_RATIO = 0.5 // 斜率减小50%
  
  for (let i = WINDOW_SIZE; i < data.length - WINDOW_SIZE; i++) {
    const [angle, torque] = data[i]
    
    // 只在拧紧段(>360°)检测
    if (angle < 360) continue
    
    // 计算前后斜率
    const slope_before = (
      data[i][1] - data[i - WINDOW_SIZE][1]
    ) / (
      data[i][0] - data[i - WINDOW_SIZE][0]
    )
    
    const slope_after = (
      data[i + WINDOW_SIZE][1] - data[i][1]
    ) / (
      data[i + WINDOW_SIZE][0] - data[i][0]
    )
    
    // 检测斜率突变
    if (slope_before > 0.1 && slope_after < slope_before * SLOPE_REDUCTION_RATIO) {
      return {
        index: i,
        angle: angle,
        torque: torque,
        slopeBefore: slope_before,
        slopeAfter: slope_after
      }
    }
  }
  
  return null
}
```

---

## 3. 数据结构

### 3.1 曲线对象结构

```javascript
const CurveData = {
  id: String,              // 唯一标识
  name: String,            // 曲线名称
  status: String,          // 状态: '正常' | '异常-滑牙' | ...
  points: Number,          // 数据点数
  maxTorque: Number,       // 峰值扭矩 (N·m)
  maxAngle: Number,        // 峰值角度 (°)
  uploadTime: String,      // 上传时间
  
  // 扩展信息
  workshop: String,        // 车间
  productionLine: String,  // 线体
  toolModel: String,       // 工具型号
  snNumber: String,        // SN编号
  shift: String,           // 班次
  
  // 原始数据点数组
  data: Array<[angle, torque]>
  // 示例: [[0, 0], [1, 0], [280, 5.2], ..., [540, 28.5]]
}
```

### 3.2 特征对象结构

```javascript
const CurveFeatures = {
  // 贴合点
  seatPoint: {
    index: Number,
    angle: Number,
    torque: Number
  },
  
  // 屈服点
  yieldPoint: {
    index: Number,
    angle: Number,
    torque: Number,
    slopeBefore: Number,
    slopeAfter: Number
  } | null,
  
  // 峰值扭矩点
  maxTorquePoint: {
    angle: Number,
    torque: Number
  },
  
  // 弹性段斜率
  elasticSlope: Number // N·m/°
}
```

### 3.3 分析结果结构

```javascript
const AnalysisResult = {
  // 综合状态
  overallStatus: String, // '正常' | '存在异常'
  qualifiedCount: Number,
  abnormalCount: Number,
  qualificationRate: Number, // 0-100
  
  // 摘要
  summary: {
    title: String,
    type: String, // 'success' | 'error'
    description: String
  },
  
  // 问题诊断
  issues: Array<{
    type: String,        // '内螺纹滑牙'
    severity: String,    // '严重' | '中等' | '轻微'
    description: String,
    affectedCurves: Array<String>,
    possibleReasons: Array<String>,
    solutions: Array<String>
  }>,
  
  // 参数对比
  parameterComparison: Array<{
    curveName: String,
    maxTorque: Number,
    maxAngle: Number,
    avgSlope: Number,
    strategy: String,    // '扭矩控制' | '转角控制'
    elasticRange: String,
    plasticRange: String,
    deviation: Number,
    controlQuality: String // '优秀' | '良好' | '需改进'
  }>,
  
  // 材质分析
  materialAnalysis: {
    material: String,
    characteristics: String,
    recommendedTorque: String,
    recommendedSpeed: String,
    notes: String
  },
  
  // 批次分析
  batchAnalysis: {
    torqueStdDev: Number,
    angleStdDev: Number,
    stabilityScore: Number,
    conclusion: {
      status: String,
      type: String,
      description: String
    },
    suggestions: Array<String>
  }
}
```

---

## 4. 异常检测逻辑

### 4.1 滑牙检测 (Thread Stripping)

**物理现象**:
- 螺纹材料承载力不足
- 扭矩上升后突然下降或波动
- 曲线尾部呈波浪形

**检测条件**:
```javascript
条件1: angle > 200° && torque < 10 Nm
OR
条件2: (angle > 300°) && (torque_drop > 15 Nm)
```

**算法伪代码**:
```
FOR each data point i:
  IF angle > 200 AND torque < 10:
    RETURN True (滑牙)
  
  IF i > 10 AND angle > 300:
    torque_drop = data[i-10].torque - data[i].torque
    IF torque_drop > 15:
      RETURN True (滑牙)

RETURN False
```

### 4.2 粘滑检测 (Stick-Slip)

**物理现象**:
- 摩擦系数不稳定
- 扭矩曲线呈锯齿状
- 拧紧过程有咔吱声

**检测条件**:
```javascript
oscillation_count > 5 && amplitude > 5 Nm
```

**算法伪代码**:
```
oscillation_count = 0
window_size = 10

FOR i FROM window_size TO length-window_size:
  local_data = data[i-window_size : i+window_size]
  local_max = MAX(local_data.torque)
  local_min = MIN(local_data.torque)
  amplitude = local_max - local_min
  
  IF amplitude > 5 AND data[i].torque == local_max:
    oscillation_count++

IF oscillation_count > 5:
  RETURN True (粘滑)
```

### 4.3 缓升检测 (Slow Torque Rise)

**物理现象**:
- 连接刚度不足
- 扭矩上升缓慢
- 弹性段斜率小

**检测条件**:
```javascript
elastic_slope < 0.08 N·m/°
```

**直接判断**: 已在特征提取阶段计算

### 4.4 压溃检测 (Crushing Failure)

**物理现象**:
- 被连接件强度不足
- 曲线出现平台或双峰
- 局部塑性变形

**检测条件**:
```javascript
在360-450°范围内, 20个点内扭矩变化 < 2 Nm
```

**算法伪代码**:
```
window_size = 20

FOR i FROM window_size TO length-window_size:
  IF angle < 360 OR angle > 450:
    CONTINUE
  
  local_data = data[i-window_size : i+window_size]
  torque_range = MAX(local_data) - MIN(local_data)
  
  IF torque_range < 2:
    RETURN True (压溃)

RETURN False
```

### 4.5 断裂检测 (Bolt Fracture)

**物理现象**:
- 螺栓超过屈服极限
- 扭矩急剧上升后归零
- 清脆断裂声

**检测条件**:
```javascript
torque > 50 Nm && sudden_drop > 30 Nm
```

**算法伪代码**:
```
FOR i FROM 10 TO length-5:
  torque = data[i].torque
  next_torques = data[i+1 : i+6].torque
  avg_next = AVERAGE(next_torques)
  
  IF torque > 50 AND (torque - avg_next) > 30:
    RETURN True (断裂)

RETURN False
```

### 4.6 浮钉检测 (High Seating)

**物理现象**:
- 螺纹孔深度不足
- 贴合点延迟
- 螺钉处于浮动状态

**检测条件**:
```javascript
seat_point.angle > 400°
```

**直接判断**: 已在贴合点识别阶段计算

### 4.7 开裂检测 (Material Cracking)

**物理现象**:
- 脆性材料
- 扭矩曲线多次小跌落
- 材料开裂声

**检测条件**:
```javascript
drop_count >= 3 && drop_threshold > 3 Nm
```

**算法伪代码**:
```
drop_count = 0
threshold = 3

FOR i FROM 5 TO length-1:
  torque_drop = data[i].torque - data[i+1].torque
  
  IF torque_drop > threshold AND angle > 300:
    drop_count++

IF drop_count >= 3:
  RETURN True (开裂)
```

---

## 5. 关键点识别

### 5.1 贴合点算法

见第2.2节

### 5.2 屈服点算法

见第2.4节

### 5.3 峰值扭矩点

```javascript
// 简单最大值查找
const maxTorque = Math.max(...data.map(d => d[1]))
const maxPoint = data.find(d => d[1] === maxTorque)
```

### 5.4 三阶段划分

```javascript
const PHASES = {
  PHASE_1: { start: 0, end: 90, name: '低速认牙' },
  PHASE_2: { start: 90, end: 360, name: '快速旋入' },
  PHASE_3: { start: 360, end: 540, name: '拧紧阶段' }
}
```

---

## 6. 材质识别算法

### 6.1 决策树

```
                    elasticSlope > 0.10?
                    /                  \
                  YES                   NO
                   /                      \
            钢质材料                  elasticSlope > 0.05?
        (slope>0.10 & torque>20)       /              \
                                     YES               NO
                                      /                  \
                              maxTorque > 80?       elasticSlope > 0.03?
                              /          \            /              \
                            YES          NO          YES             NO
                             /            \           /                \
                     钢质(低碳)        铸铁      复合材料            塑料
                   (slope 0.05-0.10)  (slope 0.08-0.12)  (slope 0.03-0.08)  (slope <0.05)
                                          ↑
                                  铝合金 (slope 0.05-0.10 & torque 10-20)
```

### 6.2 JavaScript实现

```javascript
function identifyMaterial(avgSlope, avgMaxTorque) {
  let material, characteristics, recommendedTorque, recommendedSpeed, notes
  
  if (avgSlope > 0.10 && avgMaxTorque > 20) {
    material = '钢质连接件'
    characteristics = '高强度、高刚度、线性弹性段明显...'
    recommendedTorque = '25-35 N·m (M8-M10)'
    recommendedSpeed = '40-60 rpm'
    notes = '建议采用屈服点拧紧控制策略...'
  }
  else if (avgSlope > 0.05 && avgSlope <= 0.10 && avgMaxTorque >= 10 && avgMaxTorque <= 20) {
    material = '铝合金连接件'
    // ...
  }
  else if (avgSlope <= 0.05 || avgMaxTorque < 10) {
    material = '塑料连接件'
    // ...
  }
  
  return { material, characteristics, recommendedTorque, recommendedSpeed, notes }
}
```

---

## 7. 性能优化

### 7.1 算法优化

| 优化项 | 方法 | 效果 |
|--------|------|------|
| 数据抽稀 | 超过5000点时,间隔采样 | 渲染速度↑50% |
| 滑动窗口 | 局部计算代替全局 | 内存↓30% |
| 懒加载 | 按需计算特征 | 初始化↑40% |
| Web Worker | 异步分析(计划中) | 不阻塞UI |

### 7.2 渲染优化

```javascript
// ECharts配置
const option = {
  dataZoom: [{
    type: 'inside',
    start: 0,
    end: 100
  }],
  progressive: 1000,        // 渐进渲染
  progressiveThreshold: 5000, // 阈值
  animation: false          // 关闭动画(数据量大时)
}
```

### 7.3 内存优化

```javascript
// 特征提取后释放原始数据
function optimizeMemory(curves) {
  curves.forEach(curve => {
    curve.features = extractCurveFeatures(curve.data)
    delete curve.data // 保留特征,删除原始数据
  })
}
```

---

## 8. API接口

### 8.1 核心函数

```javascript
// 异常检测
detectCurveAnomalies(curve: Object): String

// 特征提取
extractCurveFeatures(data: Array): Object

// 贴合点识别
findSeatPoint(data: Array): Object

// 弹性段斜率
calculateElasticSlope(data: Array): Number

// 屈服点识别
findYieldPoint(data: Array): Object | null

// 材质识别
identifyMaterial(slope: Number, torque: Number): Object

// 批次分析
analyzeBatchConsistency(curves: Array): Object

// 报告生成
generateProfessionalReport(): String
```

### 8.2 使用示例

```javascript
// 1. 导入曲线
const curve = {
  id: 'curve_001',
  name: '采集曲线_001',
  data: [[0, 0], [1, 0], ..., [540, 28.5]]
}

// 2. 执行异常检测
const status = detectCurveAnomalies(curve)
console.log(status) // '正常' or '异常-滑牙'

// 3. 提取特征
const features = extractCurveFeatures(curve.data)
console.log(features.seatPoint)      // {angle: 280, torque: 5.2}
console.log(features.elasticSlope)   // 0.12

// 4. 材质识别
const material = identifyMaterial(0.12, 28.5)
console.log(material.material)       // '钢质连接件'

// 5. 生成报告
const report = generateProfessionalReport()
downloadFile(report, '拧紧曲线分析报告.txt')
```

---

## 9. 配置参数

### 9.1 异常检测阈值

```javascript
const ANOMALY_THRESHOLDS = {
  // 贴合点
  seatPointMinAngle: 200,          // 最小角度
  seatPointMaxAngle: 400,          // 最大角度(超过为浮钉)
  seatPointTorque: 5,              // 扭矩阈值 N·m
  
  // 滑牙
  slipAngleMin: 200,               // 检测起始角度
  slipTorqueMin: 10,               // 扭矩下限 N·m
  slipDropThreshold: 15,           // 跌落阈值 N·m
  
  // 粘滑
  stickSlipWindowSize: 10,         // 滑动窗口大小
  stickSlipCount: 5,               // 波动次数阈值
  stickSlipAmplitude: 5,           // 振幅阈值 N·m
  
  // 缓升
  slowRiseSlope: 0.08,             // 斜率上限 N·m/°
  
  // 压溃
  crushWindowSize: 20,             // 滑动窗口大小
  crushPlateau: 2,                 // 平台阈值 N·m
  crushAngleStart: 360,            // 检测起始角度
  crushAngleEnd: 450,              // 检测结束角度
  
  // 断裂
  breakTorqueMin: 50,              // 最小扭矩 N·m
  breakDropThreshold: 30,          // 跌落阈值 N·m
  
  // 开裂
  crackDropThreshold: 3,           // 跌落阈值 N·m
  crackDropCount: 3,               // 跌落次数
  crackAngleMin: 300,              // 检测起始角度
  
  // 屈服点
  yieldWindowSize: 10,             // 滑动窗口大小
  yieldSlopeReduction: 0.5,        // 斜率减小比例
  yieldSlopeMin: 0.1,              // 最小斜率 N·m/°
  yieldAngleMin: 360               // 检测起始角度
}
```

### 9.2 材质识别参数

```javascript
const MATERIAL_THRESHOLDS = {
  steel: {
    slopeMin: 0.10,
    torqueMin: 20,
    name: '钢质连接件'
  },
  aluminum: {
    slopeMin: 0.05,
    slopeMax: 0.10,
    torqueMin: 10,
    torqueMax: 20,
    name: '铝合金连接件'
  },
  plastic: {
    slopeMax: 0.05,
    torqueMax: 10,
    name: '塑料连接件'
  },
  castIron: {
    slopeMin: 0.08,
    slopeMax: 0.12,
    torqueMin: 20,
    torqueMax: 30,
    name: '铸铁'
  },
  composite: {
    slopeMin: 0.03,
    slopeMax: 0.08,
    torqueMin: 5,
    torqueMax: 15,
    name: '复合材料'
  }
}
```

---

## 10. 扩展开发

### 10.1 添加新的异常类型

```javascript
// 1. 定义检测函数
function detectNewAnomaly(data) {
  // 实现检测逻辑
  return true/false
}

// 2. 集成到主检测函数
function detectCurveAnomalies(curve) {
  // ...
  
  // 8. 新异常检测
  if (detectNewAnomaly(curve.data)) {
    anomalies.push('新异常')
  }
  
  // ...
}

// 3. 添加诊断信息
const issues = []
const newAnomalyCurves = curves.filter(c => c.status.includes('新异常'))
if (newAnomalyCurves.length > 0) {
  issues.push({
    type: '新异常类型',
    severity: '严重',
    description: '...',
    possibleReasons: [...],
    solutions: [...]
  })
}
```

### 10.2 自定义材质类型

```javascript
// 1. 添加材质定义
const CUSTOM_MATERIAL = {
  name: '自定义材质',
  slopeMin: 0.06,
  slopeMax: 0.09,
  torqueMin: 15,
  torqueMax: 25
}

// 2. 修改识别算法
function identifyMaterial(avgSlope, avgMaxTorque) {
  // ...
  
  if (avgSlope >= CUSTOM_MATERIAL.slopeMin && 
      avgSlope <= CUSTOM_MATERIAL.slopeMax &&
      avgMaxTorque >= CUSTOM_MATERIAL.torqueMin &&
      avgMaxTorque <= CUSTOM_MATERIAL.torqueMax) {
    material = CUSTOM_MATERIAL.name
    // ...
  }
  
  // ...
}
```

### 10.3 集成机器学习模型

```javascript
// 1. 训练模型(Python)
// sklearn, TensorFlow, PyTorch...

// 2. 导出模型(ONNX/TensorFlow.js)

// 3. 集成到前端
import * as tf from '@tensorflow/tfjs'

async function predictAnomaly(features) {
  const model = await tf.loadLayersModel('path/to/model.json')
  const input = tf.tensor2d([features])
  const prediction = model.predict(input)
  const result = await prediction.data()
  return result[0] // 异常概率
}

// 4. 替代规则引擎
function detectCurveAnomaliesML(curve) {
  const features = extractCurveFeatures(curve.data)
  const featureVector = [
    features.seatPoint.angle,
    features.elasticSlope,
    features.maxTorquePoint.torque,
    // ...
  ]
  
  const anomalyProb = await predictAnomaly(featureVector)
  
  if (anomalyProb > 0.8) {
    return '异常-ML检测'
  } else {
    return '正常'
  }
}
```

---

## 📚 参考资料

### 专业标准
- ISO 5393: 旋转工具性能要求和测试方法
- VDI/VDE 2862: 拧紧技术标准
- DIN EN 45545: 螺栓连接质量要求
- GB/T 16823: 螺纹紧固件拧紧试验方法

### 技术文档
- ECharts官方文档: https://echarts.apache.org/
- Vue 3官方文档: https://vuejs.org/
- Element Plus: https://element-plus.org/

### 学术论文
- "Bolt Tightening Curve Analysis Based on Machine Learning" (2022)
- "Intelligent Fault Diagnosis of Bolted Joints" (2021)
- "Torque-Angle Signature Analysis for Quality Control" (2020)

---

**文档版本**: v3.0.0  
**最后更新**: 2025-12-16  
**维护者**: 智能体助手 + 技术团队
