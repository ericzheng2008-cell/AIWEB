# 📖 Vercel部署完整指南 - 2025-12-25

## 🎯 快速开始

### 前提条件
- ✅ Node.js >= 16
- ✅ Git仓库（GitHub/GitLab/Bitbucket）
- ✅ Vercel账号

---

## 📦 方式一：Vercel CLI（推荐）

### 1. 安装Vercel CLI
```bash
npm i -g vercel
```

### 2. 登录Vercel
```bash
vercel login
```

### 3. 部署项目
```bash
# 开发环境预览
vercel

# 生产环境部署
vercel --prod
```

### 4. 一键脚本
```bash
双击运行: ⚡_一键修复并部署Vercel_2025-12-25.bat
```

---

## 🔗 方式二：Git集成（自动部署）

### 1. 推送到GitHub
```bash
git add .
git commit -m "🚀 Deploy to Vercel"
git push origin main
```

### 2. Vercel导入项目
1. 访问 [vercel.com/new](https://vercel.com/new)
2. 选择Git仓库
3. 点击"Import"
4. 配置构建设置（自动检测）

### 3. 自动部署
- ✅ 每次推送自动触发
- ✅ PR预览环境
- ✅ 生产环境保护

---

## ⚙️ 构建配置

### vercel.json
```json
{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vue",
  "routes": [
    {
      "src": "/assets/(.*)",
      "dest": "/assets/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
```

### package.json
```json
{
  "scripts": {
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

---

## 🔧 环境变量配置

### Vercel控制台设置
1. 进入项目Settings
2. 点击Environment Variables
3. 添加变量：

```bash
VITE_API_BASE_URL=https://api.example.com
VITE_APP_TITLE=明升AIWEB
NODE_VERSION=18
```

### 本地测试
```bash
# .env.production
VITE_API_BASE_URL=https://api.mingsheng.com
```

---

## 🌐 自定义域名

### 1. 添加域名
1. 进入项目Settings → Domains
2. 添加域名：`www.mingsheng.com`
3. 配置DNS记录

### 2. DNS配置
```
类型: CNAME
名称: www
值: cname.vercel-dns.com
```

### 3. SSL证书
- ✅ 自动签发
- ✅ 自动续期
- ✅ 强制HTTPS

---

## 📊 性能优化

### 1. 构建优化
```javascript
// vite.config.js
export default defineConfig({
  build: {
    sourcemap: false,
    minify: 'terser',
    chunkSizeWarningLimit: 2000,
    rollupOptions: {
      output: {
        manualChunks(id) {
          if (id.includes('node_modules')) {
            return 'vendor'
          }
        }
      }
    }
  }
})
```

### 2. 缓存策略
```json
{
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
```

### 3. 预渲染优化
- ✅ 静态资源CDN
- ✅ 图片自动优化
- ✅ Gzip/Brotli压缩

---

## 🐛 常见问题

### 问题1: 刷新页面404
**原因**: 路由配置错误  
**解决**: 确保`vercel.json`配置正确

### 问题2: 构建超时
**原因**: 依赖包过大  
**解决**: 优化依赖，使用CDN

### 问题3: 环境变量不生效
**原因**: 变量名错误或重新部署  
**解决**: 检查变量名，触发重新部署

### 问题4: 静态资源404
**原因**: 路径配置错误  
**解决**: 检查`base`配置和路由

---

## 📈 监控与分析

### 1. Vercel Analytics
```javascript
// 自动启用
// 查看: vercel.com/[team]/[project]/analytics
```

### 2. 性能监控
- Web Vitals
- Core Web Vitals
- 加载时间分析

### 3. 日志查看
```bash
# 实时日志
vercel logs

# 查看特定部署
vercel logs [deployment-url]
```

---

## 🔄 回滚部署

### 方式一：控制台回滚
1. 进入Deployments
2. 选择历史版本
3. 点击"Promote to Production"

### 方式二：CLI回滚
```bash
# 查看部署历史
vercel ls

# 回滚到指定版本
vercel alias set [deployment-url] [alias]
```

---

## 🚨 紧急修复流程

### 1. 快速回滚
```bash
# 立即回滚到上一版本
vercel alias set [previous-deployment] production
```

### 2. 热修复部署
```bash
# 修复代码
git add .
git commit -m "🔧 Hotfix: [issue]"
git push

# 或使用CLI立即部署
vercel --prod
```

---

## 📋 部署检查清单

### 部署前
- [ ] 代码已提交到Git
- [ ] 本地构建测试通过
- [ ] 环境变量已配置
- [ ] 域名DNS已配置

### 部署后
- [ ] 生产环境访问正常
- [ ] 路由跳转测试通过
- [ ] 静态资源加载正常
- [ ] API接口正常
- [ ] 移动端适配正常

---

## 🎓 最佳实践

### 1. 分支策略
```
main分支 → 生产环境
develop分支 → 预览环境
feature分支 → PR预览
```

### 2. 版本标记
```bash
git tag v1.0.0
git push origin v1.0.0
```

### 3. 部署保护
- ✅ 启用部署保护
- ✅ 配置审批流程
- ✅ 限制部署权限

---

## 🔗 有用链接

- 📚 [Vercel官方文档](https://vercel.com/docs)
- 🎯 [Vue部署指南](https://vitejs.dev/guide/static-deploy.html)
- 💬 [Vercel社区](https://github.com/vercel/vercel/discussions)
- 🐛 [问题反馈](https://github.com/vercel/vercel/issues)

---

## 💡 高级技巧

### 1. 多环境部署
```bash
# 开发环境
vercel --env=development

# 测试环境
vercel --env=staging

# 生产环境
vercel --prod
```

### 2. 预览部署
```bash
# 生成预览链接
vercel

# 分享给团队
vercel alias [deployment-url] preview-[feature]
```

### 3. 性能分析
```bash
# 构建分析
npm run build -- --report

# Lighthouse分析
vercel inspect [deployment-url]
```

---

**文档版本**: v1.0  
**更新日期**: 2025-12-25  
**维护者**: AI Assistant
