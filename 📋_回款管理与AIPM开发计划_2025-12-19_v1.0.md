# 📋 AIPM + AIMES 完整开发计划 V1.0
**日期**: 2025-12-19  
**版本**: V1.0  
**基础**: AI MES助手(AIMES)完整设计框架

---

## 一、产品定位与核心价值

### 1.1 AIPM的新定位
> **从"项目管理工具" → "AI驱动的制造执行与项目管理平台"**

**核心差异化**:
- ❌ 不是会聊天的MS Project
- ✅ 能判断、能预测、能执行、能闭环的智能助手

### 1.2 五大核心能力升级
1. **感知能力**: 实时监控 → AI异常检测(15秒内)
2. **决策能力**: 人工排产 → AI智能调度(3套方案)
3. **预测能力**: 事后分析 → 提前3-5天预警
4. **执行能力**: 任务提醒 → 自动闭环跟踪
5. **学习能力**: 静态数据 → 成功模式沉淀

---

## 二、P2开发路线图(12周完整计划)

### 📅 Week 1-2: 数据底座建设(Data Foundation)

#### 目标
建立支持AI分析的完整数据快照体系

#### 核心交付物
1. **项目快照引擎**
```javascript
// 核心数据结构
const ProjectSnapshot = {
  projectId: String,
  snapshotTime: Date,
  progress: Number,
  budget: { planned: Number, spent: Number },
  team: { size: Number, allocation: Array },
  risks: Array,
  quality: { defectRate: Number, reworkRate: Number },
  resources: { equipment: Array, materials: Array },
  milestones: Array,
  aiPredictions: {
    delayProbability: Number,
    costOverrunRisk: Number,
    qualityTrend: String
  }
}
```

2. **时序数据仓库**
```javascript
const TimeSeriesDB = {
  daily: [],    // 每日快照
  weekly: [],   // 每周汇总
  monthly: []   // 月度总结
}
```

3. **指标计算引擎**
- OEE(设备综合效率)
- SPI(进度绩效指数) = EV / PV
- CPI(成本绩效指数) = EV / AC
- 关键路径识别
- 瓶颈工序分析

---

### 📅 Week 3-4: AI智能预测引擎(Prediction Engine)

#### 目标
实现项目风险的提前3-5天预警

#### 核心交付物

1. **延误预测模型**
```javascript
const DelayPredictionModel = {
  // 输入特征
  features: [
    'currentProgress',      // 当前进度
    'plannedProgress',      // 计划进度
    'teamVelocity',        // 团队速度
    'blockerCount',        // 阻塞问题数
    'resourceAvailability', // 资源可用性
    'historicalPattern'     // 历史模式
  ],
  
  // 输出预测
  predict() {
    return {
      delayDays: Number,           // 预计延误天数
      confidence: Number,          // 置信度
      criticalTasks: [],           // 关键任务
      recommendations: []          // AI建议
    }
  }
}
```

2. **成本超支预测**
```javascript
const CostPredictionModel = {
  predictOverrun(project) {
    const burnRate = project.spent / project.elapsedDays
    const remainingBudget = project.budget - project.spent
    const remainingDays = project.totalDays - project.elapsedDays
    
    const predictedSpent = burnRate * remainingDays
    const overrunRisk = predictedSpent > remainingBudget ? 'HIGH' : 'LOW'
    
    return {
      predictedTotal: project.spent + predictedSpent,
      overrunAmount: Math.max(0, predictedSpent - remainingBudget),
      riskLevel: overrunRisk,
      suggestion: overrunRisk === 'HIGH' 
        ? '建议优化资源分配或调整范围' 
        : '成本控制良好'
    }
  }
}
```

3. **资源冲突检测**
```javascript
const ResourceConflictDetector = {
  detect(projects) {
    const conflicts = []
    
    // 检测人员冲突
    const personMap = {}
    projects.forEach(p => {
      p.team.forEach(member => {
        if (!personMap[member.id]) {
          personMap[member.id] = []
        }
        personMap[member.id].push({
          project: p.name,
          allocation: member.allocation,
          period: p.period
        })
      })
    })
    
    // 识别超负荷人员
    Object.keys(personMap).forEach(personId => {
      const allocations = personMap[personId]
      const totalAllocation = allocations.reduce((sum, a) => sum + a.allocation, 0)
      
      if (totalAllocation > 100) {
        conflicts.push({
          type: 'PERSON_OVERLOAD',
          person: personId,
          overload: totalAllocation - 100,
          projects: allocations.map(a => a.project),
          suggestion: `${personId} 负载${totalAllocation}%，建议调整或增加人手`
        })
      }
    })
    
    return conflicts
  }
}
```

---

### 📅 Week 5-6: 数据治理模块(Data Governance)

#### 目标
确保数据质量,建立自动化清洗机制

#### 核心交付物

1. **数据质量检查器**
```javascript
const DataQualityChecker = {
  rules: [
    {
      field: 'progress',
      check: (val) => val >= 0 && val <= 100,
      fix: (val) => Math.max(0, Math.min(100, val)),
      message: '进度必须在0-100之间'
    },
    {
      field: 'startDate',
      check: (val, project) => new Date(val) <= new Date(project.endDate),
      fix: null, // 需要人工确认
      message: '开始日期不能晚于结束日期'
    },
    {
      field: 'spent',
      check: (val, project) => val >= 0 && val <= project.budget * 1.5,
      fix: null,
      message: '花费异常,可能超预算50%以上'
    }
  ],
  
  validate(project) {
    const errors = []
    const warnings = []
    
    this.rules.forEach(rule => {
      const value = project[rule.field]
      if (!rule.check(value, project)) {
        if (rule.fix) {
          warnings.push({
            field: rule.field,
            message: rule.message,
            autoFixed: true,
            oldValue: value,
            newValue: rule.fix(value)
          })
          project[rule.field] = rule.fix(value)
        } else {
          errors.push({
            field: rule.field,
            message: rule.message,
            value: value
          })
        }
      }
    })
    
    return { errors, warnings, fixed: warnings.length > 0 }
  }
}
```

2. **缺失数据补全**
```javascript
const DataImputation = {
  // 智能补全团队规模
  imputeTeamSize(project) {
    if (!project.teamSize) {
      // 基于预算估算
      const avgCostPerPerson = 10 // 万元/人/月
      const months = (new Date(project.endDate) - new Date(project.startDate)) / (1000*60*60*24*30)
      project.teamSize = Math.max(1, Math.round(project.budget / avgCostPerPerson / months))
    }
  },
  
  // 补全关键路径
  imputeCriticalPath(project) {
    if (!project.criticalPath) {
      // 根据WBS生成
      if (project.wbs && project.wbs.length > 0) {
        const longestPath = project.wbs
          .sort((a,b) => b.duration - a.duration)
          .slice(0, 3)
          .map(task => task.name)
          .join(' -> ')
        project.criticalPath = longestPath
      } else {
        project.criticalPath = '待定义'
      }
    }
  }
}
```

---

### 📅 Week 7-8: 行为驱动客户模型(Behavior Model)

#### 目标
从历史项目中学习成功模式

#### 核心交付物

1. **成功模式提取器**
```javascript
const SuccessPatternExtractor = {
  analyze(completedProjects) {
    const successful = completedProjects.filter(p => 
      p.status === '已完成' && 
      p.actualDuration <= p.plannedDuration * 1.1 &&
      p.actualCost <= p.budget
    )
    
    const patterns = {
      teamSizeRange: {
        min: Math.min(...successful.map(p => p.teamSize)),
        max: Math.max(...successful.map(p => p.teamSize)),
        avg: successful.reduce((sum, p) => sum + p.teamSize, 0) / successful.length
      },
      
      budgetRatio: {
        // 预算与团队规模的关系
        avgPerPerson: successful.reduce((sum, p) => 
          sum + p.budget / p.teamSize, 0) / successful.length
      },
      
      commonCriticalPath: this.extractCommonPattern(
        successful.map(p => p.criticalPath)
      ),
      
      riskManagement: {
        avgRiskCount: successful.reduce((sum, p) => 
          sum + (p.risks?.length || 0), 0) / successful.length,
        commonRisks: this.findCommonRisks(successful)
      }
    }
    
    return patterns
  },
  
  extractCommonPattern(paths) {
    // 分词统计
    const wordCount = {}
    paths.forEach(path => {
      path.split(/[->、，,]/).forEach(word => {
        word = word.trim()
        if (word) {
          wordCount[word] = (wordCount[word] || 0) + 1
        }
      })
    })
    
    // 返回高频词
    return Object.entries(wordCount)
      .sort((a,b) => b[1] - a[1])
      .slice(0, 5)
      .map(([word, count]) => ({ word, frequency: count / paths.length }))
  }
}
```

2. **智能推荐引擎**
```javascript
const SmartRecommender = {
  recommendForNewProject(newProject, successPatterns) {
    const recommendations = []
    
    // 团队规模建议
    if (!newProject.teamSize) {
      const recommended = Math.round(
        newProject.budget / successPatterns.budgetRatio.avgPerPerson
      )
      recommendations.push({
        type: 'TEAM_SIZE',
        title: '团队规模建议',
        content: `基于${newProject.budget}万预算，建议配置${recommended}人团队`,
        confidence: 0.85
      })
    }
    
    // 关键路径建议
    if (!newProject.criticalPath || newProject.criticalPath === '待定义') {
      const commonPath = successPatterns.commonCriticalPath
        .map(p => p.word)
        .join(' -> ')
      recommendations.push({
        type: 'CRITICAL_PATH',
        title: '关键路径建议',
        content: `参考成功项目，建议路径：${commonPath}`,
        confidence: 0.75
      })
    }
    
    // 风险预警
    const avgRiskCount = successPatterns.riskManagement.avgRiskCount
    recommendations.push({
      type: 'RISK_ALERT',
      title: '风险管理建议',
      content: `成功项目平均识别${avgRiskCount.toFixed(1)}个风险，建议提前规划`,
      confidence: 0.90
    })
    
    return recommendations
  }
}
```

---

### 📅 Week 9-10: 业务模拟器(What-If Simulator)

#### 目标
支持多种场景的模拟分析

#### 核心交付物

1. **场景模拟引擎**
```javascript
const WhatIfSimulator = {
  scenarios: {
    // 场景1: 增加人手
    addTeamMember(project, count = 1) {
      const newProject = JSON.parse(JSON.stringify(project))
      newProject.teamSize += count
      
      // 假设每增加1人,进度加快5%
      const speedup = count * 0.05
      const remainingDays = (new Date(project.endDate) - new Date()) / (1000*60*60*24)
      const newRemainingDays = remainingDays * (1 - speedup)
      
      const newEndDate = new Date()
      newEndDate.setDate(newEndDate.getDate() + newRemainingDays)
      
      newProject.endDate = newEndDate.toISOString().split('T')[0]
      newProject.budget += count * 10 // 每人增加10万成本
      
      return {
        scenario: `增加${count}名团队成员`,
        changes: {
          teamSize: { old: project.teamSize, new: newProject.teamSize },
          endDate: { old: project.endDate, new: newProject.endDate },
          budget: { old: project.budget, new: newProject.budget }
        },
        impact: {
          schedule: `提前${Math.round(remainingDays - newRemainingDays)}天`,
          cost: `增加${count * 10}万`,
          roi: newRemainingDays < remainingDays ? 'POSITIVE' : 'NEGATIVE'
        }
      }
    },
    
    // 场景2: 砍掉非关键任务
    removeNonCritical(project) {
      const newProject = JSON.parse(JSON.stringify(project))
      const nonCritical = newProject.wbs.filter(task => task.priority !== 'HIGH')
      const removed = nonCritical.length
      
      newProject.wbs = newProject.wbs.filter(task => task.priority === 'HIGH')
      
      // 假设去掉30%工作量
      const workReduction = removed / (removed + newProject.wbs.length)
      const remainingDays = (new Date(project.endDate) - new Date()) / (1000*60*60*24)
      const newRemainingDays = remainingDays * (1 - workReduction * 0.3)
      
      const newEndDate = new Date()
      newEndDate.setDate(newEndDate.getDate() + newRemainingDays)
      
      newProject.endDate = newEndDate.toISOString().split('T')[0]
      
      return {
        scenario: `精简非关键任务(${removed}项)`,
        changes: {
          tasks: { old: project.wbs.length, new: newProject.wbs.length },
          endDate: { old: project.endDate, new: newProject.endDate }
        },
        impact: {
          schedule: `提前${Math.round(remainingDays - newRemainingDays)}天`,
          risk: '范围缩减,需重新评审需求'
        }
      }
    },
    
    // 场景3: 延长工期
    extendDeadline(project, days = 7) {
      const newProject = JSON.parse(JSON.stringify(project))
      const newEndDate = new Date(project.endDate)
      newEndDate.setDate(newEndDate.getDate() + days)
      
      newProject.endDate = newEndDate.toISOString().split('T')[0]
      
      // 假设可以优化质量或降低成本
      const qualityImprovement = days * 0.02 // 每延长1天,质量提升2%
      
      return {
        scenario: `延长交期${days}天`,
        changes: {
          endDate: { old: project.endDate, new: newProject.endDate }
        },
        impact: {
          schedule: `延后${days}天`,
          quality: `质量提升约${(qualityImprovement * 100).toFixed(1)}%`,
          cost: '无额外成本'
        }
      }
    }
  },
  
  // 批量模拟
  runMultipleScenarios(project) {
    return {
      baseline: project,
      scenarios: [
        this.scenarios.addTeamMember(project, 2),
        this.scenarios.removeNonCritical(project),
        this.scenarios.extendDeadline(project, 7)
      ]
    }
  }
}
```

---

### 📅 Week 11-12: AI助手集成与优化

#### 目标
将所有智能能力整合为统一的对话式助手

#### 核心交付物

1. **AI助手Prompt**
```javascript
const AIPMAssistantPrompt = `
你是AIPM智能项目管理助手,具备以下能力:

1. 项目分析
   - 识别延误风险
   - 预测成本超支
   - 发现资源冲突

2. 智能建议
   - 提供3套排产方案
   - 推荐团队配置
   - 建议风险应对

3. 场景模拟
   - What-If分析
   - 多方案对比
   - ROI评估

4. 知识沉淀
   - 学习成功模式
   - 提取最佳实践
   - 推荐类似案例

用户问题类型:
- "为什么项目A进度落后?"
- "如果增加2个人,能提前多少天?"
- "这个项目最大的风险是什么?"
- "给我3套加急方案"

回答要求:
1. 结构化输出(数据+建议+理由)
2. 量化结果(天数/成本/概率)
3. 可执行方案(具体步骤)
4. 风险提示
`
```

2. **集成示例**
```javascript
const AIPMAssistant = {
  async query(question, context) {
    // 意图识别
    const intent = this.classifyIntent(question)
    
    switch(intent) {
      case 'DELAY_ANALYSIS':
        return this.analyzeDelay(context.project)
        
      case 'WHAT_IF':
        const params = this.extractSimulationParams(question)
        return WhatIfSimulator.scenarios[params.scenario](
          context.project, 
          params.value
        )
        
      case 'RISK_PREDICTION':
        return DelayPredictionModel.predict(context.project)
        
      case 'RECOMMENDATION':
        return SmartRecommender.recommendForNewProject(
          context.project,
          context.successPatterns
        )
        
      default:
        return '抱歉,我还不理解这个问题'
    }
  },
  
  classifyIntent(question) {
    if (question.includes('为什么') && question.includes('延误')) {
      return 'DELAY_ANALYSIS'
    }
    if (question.includes('如果') || question.includes('增加')) {
      return 'WHAT_IF'
    }
    if (question.includes('风险') || question.includes('预测')) {
      return 'RISK_PREDICTION'
    }
    if (question.includes('建议') || question.includes('推荐')) {
      return 'RECOMMENDATION'
    }
    return 'UNKNOWN'
  }
}
```

---

## 三、核心数据结构(完整版)

### 3.1 项目快照数据模型
```typescript
interface ProjectSnapshot {
  // 基础信息
  id: string
  projectId: string
  snapshotTime: Date
  
  // 进度数据
  progress: {
    planned: number      // 计划进度%
    actual: number       // 实际进度%
    spi: number         // 进度绩效指数
  }
  
  // 成本数据
  budget: {
    total: number       // 总预算
    spent: number       // 已花费
    cpi: number        // 成本绩效指数
    ev: number         // 挣值
    pv: number         // 计划值
    ac: number         // 实际成本
  }
  
  // 团队数据
  team: {
    size: number
    allocation: Array<{
      memberId: string
      role: string
      allocation: number  // 分配比例%
    }>
    velocity: number     // 团队速度(任务/天)
  }
  
  // 风险数据
  risks: Array<{
    id: string
    type: string        // SCHEDULE/COST/QUALITY/RESOURCE
    severity: string    // LOW/MEDIUM/HIGH
    probability: number // 0-1
    impact: string
    mitigation: string
  }>
  
  // 质量数据
  quality: {
    defectRate: number      // 缺陷率
    reworkRate: number      // 返工率
    testCoverage: number    // 测试覆盖率
  }
  
  // AI预测
  aiPredictions: {
    delayProbability: number       // 延误概率
    delayDays: number             // 预计延误天数
    costOverrunRisk: string       // 成本风险等级
    predictedEndDate: Date        // 预测完成日期
    confidence: number            // 预测置信度
    recommendations: Array<string>
  }
}
```

---

## 四、UI/UX优化方案

### 4.1 新增功能入口

**位置**: AIPM标签页顶部工具栏

```vue
<el-button-group>
  <el-button type="primary" @click="showAIPrediction">
    <el-icon><TrendCharts /></el-icon>
    AI预测
  </el-button>
  <el-button type="success" @click="showWhatIfSimulator">
    <el-icon><Tools /></el-icon>
    场景模拟
  </el-button>
  <el-button type="warning" @click="showDataQuality">
    <el-icon><DataAnalysis /></el-icon>
    数据质量
  </el-button>
  <el-button type="info" @click="showSuccessPatterns">
    <el-icon><Collection /></el-icon>
    成功模式
  </el-button>
</el-button-group>
```

### 4.2 AI预测面板
```vue
<el-drawer v-model="showPredictionDrawer" title="AI智能预测" size="50%">
  <el-timeline>
    <el-timeline-item 
      v-for="prediction in aiPredictions" 
      :key="prediction.id"
      :type="prediction.severity"
    >
      <el-card>
        <template #header>
          <div class="prediction-header">
            <el-icon :color="prediction.color"><Warning /></el-icon>
            <span>{{ prediction.title }}</span>
            <el-tag :type="prediction.severity">
              置信度: {{ (prediction.confidence * 100).toFixed(0) }}%
            </el-tag>
          </div>
        </template>
        
        <div class="prediction-content">
          <p><strong>预测结果:</strong> {{ prediction.result }}</p>
          <p><strong>关键因素:</strong></p>
          <ul>
            <li v-for="factor in prediction.factors" :key="factor">
              {{ factor }}
            </li>
          </ul>
          <p><strong>AI建议:</strong></p>
          <el-tag 
            v-for="(rec, idx) in prediction.recommendations" 
            :key="idx"
            style="margin: 5px"
          >
            {{ rec }}
          </el-tag>
        </div>
        
        <template #footer>
          <el-button size="small" type="primary" @click="applyRecommendation(prediction)">
            采纳建议
          </el-button>
        </template>
      </el-card>
    </el-timeline-item>
  </el-timeline>
</el-drawer>
```

---

## 五、开发优先级与验收标准

### P0 (Week 1-2) - 必须完成
- [ ] 项目快照引擎
- [ ] 时序数据仓库
- [ ] 基础指标计算(OEE/SPI/CPI)

**验收标准**:
- 每日自动生成快照
- 历史数据可查询
- 3大指标实时计算

### P1 (Week 3-4) - 高优先级
- [ ] 延误预测模型
- [ ] 成本预测模型
- [ ] 资源冲突检测

**验收标准**:
- 延误预测提前3天
- 成本预测误差<10%
- 冲突检测覆盖人/设备/物料

### P2 (Week 5-12) - 中长期
- [ ] 数据质量检查
- [ ] 成功模式提取
- [ ] What-If模拟器
- [ ] AI助手集成

**验收标准**:
- 数据质量分>90分
- 成功模式库>20条
- 模拟器支持5种场景
- AI助手准确率>85%

---

## 六、技术栈与工具选型

### 前端
- Vue 3 + Composition API
- ECharts(甘特图/雷达图/时间轴)
- Element Plus
- Day.js(时间处理)

### 数据处理
- LocalStorage(临时存储)
- IndexedDB(大量快照数据)
- Web Worker(后台计算)

### AI能力
- TensorFlow.js(时序预测)
- ML5.js(轻量化模型)
- 自研规则引擎

---

## 七、风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 模型不准确 | 中 | 高 | 规则兜底+在线学习 |
| 性能问题 | 低 | 中 | Web Worker+分页加载 |
| 用户不理解AI | 中 | 中 | 可解释性+置信度展示 |
| 数据质量差 | 高 | 高 | 自动清洗+人工审核 |

---

## 八、下一步行动

### 立即开始(今天)
1. 阅读完整文档
2. 创建Week 1-2开发分支
3. 实现项目快照引擎

### 本周完成
- P0全部功能
- 数据结构定义
- 单元测试编写

### 两周验收
- 完整的数据快照体系
- 3大核心指标可视化
- 测试覆盖率>80%

---

**文档版本**: V1.0  
**下次更新**: 2025-12-26(P0完成后)  
**负责人**: AIPM开发组  
**联系方式**: [填写]
