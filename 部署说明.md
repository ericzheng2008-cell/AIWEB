# 网站部署说明

## ✅ 构建完成

项目已成功构建，输出在 `dist` 文件夹中。

## 🚀 快速部署方式

### 方案1：Netlify Drop (最简单 - 推荐)

1. 访问：https://app.netlify.com/drop
2. 将 `dist` 文件夹直接拖拽到网页中
3. 等待几秒钟，即可获得可访问的网址
4. 网址格式：`https://xxxxxx.netlify.app`

**优点**：
- 完全免费
- 无需注册账号（可选）
- 拖拽即可部署
- 自动提供 HTTPS
- 全球 CDN 加速

### 方案2：Vercel CLI

```bash
# 安装 Vercel CLI
npm install -g vercel

# 部署
vercel --prod
```

### 方案3：GitHub Pages

```bash
# 推送到 GitHub
git add .
git commit -m "部署网站"
git push origin main

# 在 GitHub 仓库设置中启用 GitHub Pages
# Settings -> Pages -> Source: gh-pages branch
```

### 方案4：本地局域网访问

如果只需要让同事在局域网内访问：

```bash
npm run dev
```

然后分享给同事：`http://你的IP:3002`

例如：`http://192.168.1.100:3002`

## 📝 获取当前 IP 地址

Windows PowerShell:
```powershell
ipconfig | findstr IPv4
```

## 🔗 访问链接说明

部署后，您可以将获得的网址分享给同事：
- 可以通过任何设备访问
- 支持手机、平板、电脑
- 全球任何地方都可以访问

## 📞 技术支持

如有问题，请查看控制台输出或联系技术支持。
