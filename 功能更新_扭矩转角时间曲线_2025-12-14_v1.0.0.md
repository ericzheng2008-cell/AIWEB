# 功能更新说明 - 扭矩转角-时间曲线

**版本**: v1.0.0  
**日期**: 2025-12-14  
**更新模块**: 拧紧数据采集分析系统、拧紧曲线对比分析系统

---

## 📋 更新概述

在拧紧数据采集分析页面和曲线分析页面的曲线图下拉菜单中，新增了"**扭矩转角-时间**"曲线类型，用于同时展示扭矩和角度随时间的变化关系，提供更全面的拧紧过程综合分析。

---

## ✨ 新增功能

### 1. 扭矩转角-时间曲线（双Y轴显示）

#### 功能描述
- **纵坐标（左）**: 扭矩（Nm）
- **纵坐标（右）**: 旋入角度（°）
- **横坐标**: 时间（s）
- **曲线类型**: 双Y轴折线图，同时显示扭矩和角度随时间的变化

#### 技术特性
- **双Y轴设计**: 左侧Y轴显示扭矩（0-40Nm），右侧Y轴显示角度（0-600°）
- **时间轴范围**: 0-5秒，覆盖完整拧紧过程
- **阶段标识**: 自动标注三个拧紧阶段
  - ① 低速认牙阶段（0-0.5s）
  - ② 快速旋入阶段（0.5-2.5s）
  - ③ 拧紧阶段（2.5-4.5s）

#### 视觉效果
- **扭矩曲线**: 红色实线 (#f56c6c)，带半透明渐变填充
- **角度曲线**: 蓝色虚线 (#409eff)
- **交互提示**: 十字准星，实时显示时间、扭矩、角度和当前阶段

---

## 🎯 应用场景

### 适用场景
1. **拧紧过程综合分析**: 同时观察扭矩和角度的变化趋势
2. **工艺参数验证**: 验证扭矩控制策略和转角控制策略的执行情况
3. **异常检测**: 快速识别扭矩与角度不匹配的异常情况
4. **时序分析**: 分析拧紧过程中扭矩和角度随时间的协调关系

### 典型分析示例

#### 正常拧紧过程
```
时间: 0-0.5s   → 扭矩缓慢上升，角度线性增加（认牙阶段）
时间: 0.5-2.5s → 扭矩平稳，角度快速上升（旋入阶段）
时间: 2.5-4.5s → 扭矩快速上升至目标值，角度增速减缓（拧紧阶段）
```

#### 异常情况识别
- **滑牙**: 角度持续增加，但扭矩突然下降或停滞
- **浮钉**: 扭矩快速上升后停滞，角度不再增加
- **粘滑**: 扭矩和角度呈锯齿状波动
- **压溃**: 扭矩曲线出现平台，角度继续增加

---

## 📊 技术实现

### 1. 数据转换算法

```javascript
// 将角度-扭矩数据转换为时间序列数据
const convertToTimeData = (angleData) => {
  const timeData = []        // 时间-扭矩数据
  const angleTimeData = []   // 时间-角度数据
  
  angleData.forEach(([angle, torque]) => {
    let time = 0
    
    // 根据角度计算对应的时间
    if (angle <= 90) {
      time = angle / 180              // 低速认牙（180°/s）
    } else if (angle <= 360) {
      time = 0.5 + (angle - 90) / 135 // 快速旋入（135°/s）
    } else {
      time = 2.5 + (angle - 360) / 90 // 拧紧阶段（90°/s）
    }
    
    timeData.push([parseFloat(time.toFixed(3)), torque])
    angleTimeData.push([parseFloat(time.toFixed(3)), angle])
  })
  
  return { timeData, angleTimeData }
}
```

### 2. ECharts双Y轴配置

```javascript
yAxis: [
  {
    type: 'value',
    name: '扭矩 (Nm)',
    position: 'left',
    min: 0,
    max: 40,
    axisLabel: { formatter: '{value} Nm' }
  },
  {
    type: 'value',
    name: '旋入角度 (°)',
    position: 'right',
    min: 0,
    max: 600,
    axisLabel: { formatter: '{value}°' }
  }
]
```

### 3. 交互提示增强

```javascript
tooltip: {
  trigger: 'axis',
  axisPointer: { type: 'cross' },
  formatter: (params) => {
    const time = params[0].axisValue
    // 判断拧紧阶段
    let phase = time <= 0.5 ? '低速认牙阶段' : 
                time <= 2.5 ? '快速旋入阶段' : '拧紧阶段'
    
    return `时间: ${time}s
            阶段: ${phase}
            扭矩: ${params[0].value} Nm
            转角: ${params[1].value}°`
  }
}
```

---

## 🔧 修改文件清单

### 1. TighteningDataAnalysis.vue (拧紧数据采集分析页面)
- **位置**: `src/views/admin/TighteningDataAnalysis.vue`
- **修改内容**:
  - 第517-538行: 下拉菜单增加"扭矩转角-时间"选项
  - 第2047-2098行: 新增扭矩转角-时间曲线生成逻辑（双Y轴）
  - 第2060-2067行: 优化曲线类型切换提示信息

### 2. CurveAnalysis.vue (拧紧曲线对比分析页面)
- **位置**: `src/views/CurveAnalysis.vue`
- **修改内容**:
  - 第162-183行: 下拉菜单增加"扭矩转角-时间"选项
  - 第1022-1040行: 新增`convertToTimeData`数据转换函数
  - 第1066-1206行: 重构`updateChart`函数，支持双Y轴模式
  - 第1208-1309行: 重构`updateCurveType`函数，支持双Y轴配置

---

## 📖 使用指南

### 操作步骤

#### 在拧紧数据采集分析页面
1. 进入系统 → 后台管理 → 拧紧数据采集分析
2. 点击"拧紧曲线"标签页
3. 在曲线图右上角的下拉菜单中选择"**扭矩转角-时间**"
4. 图表自动切换为双Y轴显示模式

#### 在拧紧曲线对比分析页面
1. 进入智能体 → 拧紧曲线对比与分析
2. 导入曲线数据（标准曲线 + 采集曲线）
3. 在曲线图右上角的下拉菜单中选择"**扭矩转角-时间**"
4. 图表同时显示所有选中曲线的扭矩和角度变化

### 分析要点

#### 1. 扭矩-角度协同性
- **正常情况**: 扭矩和角度曲线保持协调上升
- **异常情况**: 扭矩上升时角度停滞，或角度增加时扭矩下降

#### 2. 拧紧速度识别
- **阶段1（0-0.5s）**: 角度斜率小，扭矩缓升 → 低速认牙
- **阶段2（0.5-2.5s）**: 角度斜率大，扭矩平稳 → 快速旋入
- **阶段3（2.5-4.5s）**: 角度斜率减小，扭矩快速上升 → 拧紧控制

#### 3. 控制策略判断
- **扭矩控制**: 扭矩曲线平稳上升至目标值，角度波动±20°
- **转角控制**: 角度曲线平稳上升至目标值，扭矩相对稳定

---

## 🔍 对比其他曲线类型

| 曲线类型 | 横坐标 | 纵坐标 | 适用场景 | 优势 |
|---------|--------|--------|---------|------|
| **扭矩-角度** | 旋入角度 (°) | 扭矩 (Nm) | 材质分析、异常检测 | 最直观，默认曲线 |
| **扭矩-时间** | 时间 (s) | 扭矩 (Nm) | 时序分析、速度评估 | 看清拧紧过程的时间分布 |
| **扭矩-转速** | 转速 (rpm) | 扭矩 (Nm) | 动力学分析 | 识别不同转速下的扭矩特性 |
| **角度-时间** | 时间 (s) | 旋入角度 (°) | 进程分析 | 评估拧紧进度和速度变化 |
| **扭矩转角-时间** ⭐ | 时间 (s) | 扭矩+角度 | **综合分析** | **同时观察扭矩和角度，最全面** |

---

## 💡 最佳实践

### 1. 工艺验证
```
使用场景: 验证拧紧策略是否按预期执行
操作步骤:
  1. 导入标准曲线和实际采集曲线
  2. 切换到"扭矩转角-时间"视图
  3. 对比扭矩和角度的时序关系
  4. 检查是否存在扭矩过早达到目标值或角度超限
```

### 2. 异常诊断
```
使用场景: 快速识别拧紧异常类型
分析方法:
  • 滑牙: 观察扭矩达到峰值后下降，角度继续增加
  • 浮钉: 扭矩快速上升后停滞，角度不再变化
  • 粘滑: 扭矩和角度曲线均呈锯齿状波动
  • 压溃: 扭矩出现平台，角度继续上升
```

### 3. 批次对比
```
使用场景: 评估不同批次的拧紧一致性
操作步骤:
  1. 导入多条曲线（≤10条，避免图表过于拥挤）
  2. 切换到"扭矩转角-时间"视图
  3. 观察扭矩和角度曲线是否高度重合
  4. 分析批次间的工艺稳定性
```

---

## 📈 示例数据解读

### 正常拧紧曲线特征

```
时间轴分析:
  0.0-0.5s  → 扭矩: 0 → 2Nm   | 角度: 0 → 90°   → 低速认牙
  0.5-2.5s  → 扭矩: 2 → 6Nm   | 角度: 90 → 360° → 快速旋入
  2.5-4.5s  → 扭矩: 6 → 35Nm  | 角度: 360 → 540° → 拧紧到位

曲线特征:
  • 扭矩曲线: 前2.5s缓慢上升，2.5s后快速上升至目标扭矩
  • 角度曲线: 0.5-2.5s快速上升（高速旋入），2.5s后减速
  • 协调性: 扭矩上升期对应角度减速期，符合拧紧物理过程
```

---

## ⚠️ 注意事项

### 1. 数据转换精度
- 时间计算基于理想拧紧速度模型（180 rpm / 800 rpm / 300 rpm）
- 实际拧紧速度可能与模型存在偏差，仅供参考

### 2. 图表性能
- 建议同时显示的曲线数量≤10条
- 双Y轴模式下每条曲线生成2条series（扭矩+角度）
- 过多曲线会导致图表渲染性能下降和图例拥挤

### 3. 坐标轴范围
- 左Y轴（扭矩）: 0-40Nm，适用于M8-M12螺栓
- 右Y轴（角度）: 0-600°，覆盖多圈拧紧场景
- 如需调整范围，需修改ECharts配置

---

## 🔄 后续优化方向

### 1. 自适应坐标轴范围
- 根据实际数据自动调整Y轴范围
- 支持用户手动设置坐标轴上下限

### 2. 速度曲线叠加
- 增加第三条曲线显示拧紧转速变化
- 实现三Y轴显示（扭矩+角度+转速）

### 3. 相位差分析
- 计算扭矩和角度曲线的相位差
- 识别扭矩超前或滞后于角度的异常情况

### 4. 标准曲线对比
- 在双Y轴模式下同时显示标准曲线和采集曲线
- 高亮显示偏差超过阈值的区域

---

## 📝 版本记录

### v1.0.0 (2025-12-14)
- ✅ 新增"扭矩转角-时间"曲线类型
- ✅ 实现双Y轴显示（扭矩+角度）
- ✅ 支持多曲线同时显示（每条曲线生成扭矩和角度两条series）
- ✅ 增强交互提示（显示时间、阶段、扭矩、角度）
- ✅ 添加阶段标识线（认牙结束、旋入结束）
- ✅ 优化视觉效果（扭矩红色实线+填充，角度蓝色虚线）

---

## 🎓 技术参考

### 拧紧物理模型
```
阶段划分依据:
  • 低速认牙: 螺纹对齐，扭矩≤5% × 目标扭矩
  • 快速旋入: 螺栓快速旋入，扭矩基本恒定
  • 拧紧阶段: 扭矩快速上升，经历弹性段和塑性段
  
速度模型:
  • 认牙速度: 180 rpm（慢速）
  • 旋入速度: 800 rpm（高速）
  • 拧紧速度: 300 rpm（中速）
```

### ECharts双Y轴最佳实践
- 左右Y轴量纲不同时，必须设置不同的`position`
- 使用`yAxisIndex`指定series对应的Y轴（0=左，1=右）
- 建议左右Y轴使用不同颜色的`axisLabel`以区分

---

## 📞 技术支持

如有问题或建议，请联系开发团队。

**文档版本**: v1.0.0  
**最后更新**: 2025-12-14  
**作者**: AI开发助手
